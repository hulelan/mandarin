[project]
name = "claw"
version = "0.1.0"
description = "Local coding agent for open-source models"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[project.scripts]
claw = "src.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8.0"]
